# Deferred findings for joint review

From the June 2026 full-codebase review. None of these were changed. Section A items
are silent fallbacks/defaults — many exist because manufacturers don't provide the
data, so each needs an explicit keep-or-throw decision. Section B items are design
questions or medium-confidence findings that need an owner call before touching.

## A. Silent fallbacks / defaults (review one by one)

| # | Location | Behaviour today |
|---|----------|-----------------|
| A1 | `Core.cpp:1367` | Missing material saturation data → returns `defaults.magneticFluxDensitySaturation` (throw commented out; the H-sat sibling at :1400 already throws). Feeds saturation current + adviser verdicts. |
| A2 | `Wire.cpp:131,153,172` | Missing wire material → silently set to `defaults.defaultConductorMaterial` (throw commented out). |
| A3 | `Wire.cpp:1726,1741` | Missing coating thermal data → 0.25 W/(m·K) / 30 µm literature constants. Note: the type chain at 1716-1723 never handles ENAMELLED, so the most common coating always lands on the fallback. |
| A4 | `Wire.cpp:329` + `get_filling_factor`/`get_outer_dimension` | Empty interpolator data → silent return (throw commented out); callers then invoke a default-constructed `tk::spline` → out-of-bounds UB. `get_packing_factor` re-checks `contains(key)`; these two don't. At minimum needs the UB guard even if the no-throw stays. |
| A5 | `ThermalResistance.cpp:247` | Wire material with empty conductivity table → silently treated as copper. |
| A6 | `Temperature.cpp:178,212` | `catch (...)` → config-default thermal conductivities. |
| A7 | `StrayCapacitance.cpp:653-662` | Unreadable coating permittivity → εr = 3.5; PLANAR unconditionally 4.5. |
| A8 | `WindingSkinEffectLosses.cpp:632` | Dead `try/catch` (FP math never throws) → `Kc = 1.0`. |
| A9 | `CoreLosses.cpp:1029` | INVESTIGATED (June 2026): the disabled ciGSE model (unconditional nullptr at :1061, "non-physical losses") is almost certainly UNBOUNDED EXTRAPOLATION, not a unit error: Arruti TPEL Jan-2024 explicitly restricts the expanded loss space to the experimentally covered MagNet region ("the colored area represents the range where the methodology can be applied"), and a 5th-degree polynomial in (ln\|dB/dt\|, ln ΔB) explodes outside its fit domain. Re-enable plan: (1) add a domain check — evaluate only when X=ln\|dB/dt\| and Y=ln ΔB fall inside the trained region (MagNet N87: ΔB≈0.01–0.3 T, dB/dt≈1e3–1e7 T/s), loud iGSE fallback outside; (2) re-verify Table II/V coefficients against the preprint (ebiltegia.mondragon.edu handle 20.500.11984/6112) / thesis (handle 20.500.11984/6971); (3) then make the loader at :1029 rethrow. Standalone work item. |
| A10 | `Coil.cpp:7394,7407` | Missing/failed insulation material lookup → `defaults.defaultLayerInsulationMaterial` / `defaults.defaultInsulationMaterial` (throws commented out). |
| A11 | `Coil.cpp:6819` | `resolve_wire`: in-band sentinel `"Dummy"` → `"Round 0.01 - Grade 1"`. |
| A12 | `Utils.cpp:954,966,978` | `find_bobbin/insulation_material/wire_material_by_name` not-found → empty `json::parse("{}")` object (wire and core-material siblings throw). |
| A13 | `Utils.cpp:349` | `load_databases` drops malformed core materials with `catch (...) { continue; }` (the LibraryContext copy rethrows with a clear message). |
| A14 | `Utils.cpp:1472+` | INVESTIGATED (June 2026): keep as is. The only consumers (WindingSkinEffectLosses, WindingProximityEffectLosses) cap the argument at \|α\|=100 BEFORE calling, consume Bessel RATIOS, and convert any NaN/Inf result into the asymptotically-correct ratio (1e100/1e100 = 1) via their own guards. A (0,0) return becomes 0/0=NaN there and is absorbed the same way. Throwing would break the deliberate large-argument probing, and the tgammaf episode (B20b) showed this stack is fragile. The real fix is the large-argument Bessel algorithm in B20b, which replaces the whole arrangement. |
| A15 | `Utils.cpp:1854` | INVESTIGATED (June 2026): keep, but document in-code. Real chord angles max out at 180° (asin domain), so 360° is genuinely OUT-OF-BAND — a "wire does not fit on this circumference" sentinel that 45 call sites (toroidal capacity math, adviser exploration of too-small toroids) rely on to reject candidates without exceptions. Throwing would turn routine adviser exploration into exception-driven control flow. Action: add a doc comment to the function declaring 360 the documented does-not-fit return. |
| A16 | `Utils.cpp:2267` | `magnetic_autocomplete` clamps negative gap length to 0. |
| A17 | `Inputs.cpp` (`calculate_max_volt_seconds`) | Missing frequency → silent 100 kHz default. |
| A18 | `CorePiece.cpp:944` (PQ "totally made up" J/L), `:779` (RM subtype-2 b=0), `:2256` (degenerate volume → hardcoded loss fractions 0.4/0.2), EP/EPX `dimensions["K"]` vs `k=F/2` inconsistency (:529/:670) | Fabricated/silent geometry defaults. |
| A19 | `MagneticFilterArea.cpp:130` | Toroid without radial height → bobbinFillingFactor 0.6. |
| A20 | `CoilAdviser.cpp:346-356` | All designs filtered out → returns the filter-FAILED designs anyway, unsorted, indistinguishable from valid results. |
| A21 | `CoreAdviserFilters.cpp` (MagneticCoreFilterLosses) | PARTIALLY FIXED (June 2026): the silent all-rejected → pass-through is gone (genuine all-rejected now returns empty). But the investigation exposed WHY it always triggered for transformers: the dataset stage builds 1-winding dummy coils for everything except CMCs (`CoreAdviserDataset.cpp:76-110`), and the loss evaluators reject on winding-count mismatch — so the Losses filter has NEVER scored transformer candidates. Current state: multi-winding inputs pass through unscored with a WARNING log. DECISION NEEDED: implement transformer-aware loss scoring (build secondaries from requirement turns ratios before evaluating), or accept that transformers are ranked without a loss score at the CoreAdviser stage. |
| A22 | `CoreAdviserPipeline.cpp:337-367` | Zero results → reruns WITHOUT the saturation filter and returns those cores with only a log line. |
| A23 | `Mas.cpp:44-53` | `from_file`: magnetizing-inductance processing failure swallowed; file loaded without it. |
| A24 | Painter | `Painter.h:75` malformed hex color → black; `Painter.cpp:871-894` `{0,0}` pixel size / `width*2.5` "approximate toroid diameter"; `PainterImpl.cpp:2264+` hardcoded 2 mm bobbin wall / 50 µm insulation in temperature field; `:1751` per-turn losses matched positionally when names missing. |
| A25 | Circuit sim | `Interface.cpp:54` `get_saturation_parameters` catch(...) → linear inductor; `:2411` Steinmetz α=1.5 default; `:2433` unfitted FractionalPole on empty data; `Ngspice.cpp:249-265` leakage failure → 0.02·Lmag "99% coupling" + kij clamped to [0.5,0.98]; `Ltspice.cpp:117`/Ngspice twin: leakage ≥ Lmag → silent 0.1·Lmag; `NgspiceRunner.cpp:743-769` raw-file value parse errors silently skipped WITHOUT advancing the variable index (column-shift corruption); `:827` `parse_csv_output` returns success with zero waveforms; `:975-1032` unmapped waveform names silently omitted from the excitation. |

## B. Design questions / medium-confidence findings (need owner decision)

| # | Location | Question |
|---|----------|----------|
| B1 | `CoreAdviser.cpp:167-306` + `CoreAdviserPipeline.cpp:504+,761+` | User/MagneticAdviser weights are silently dead in STANDARD_CORES / CUSTOM_CORES (the default modes): `_weights` never assigned on those paths and the standard-core filter functions hardcode 1/0.1/0.001. Intended? |
| B2 | `IsolatedBuckBoost.cpp:139` | `secondaryVoltageMinimum = (Vpri_out − Vd)/n + Vd` — flagged as algebraically inconsistent with IsolatedBuck's `−(Vin − Vpri_out)/n`. Needs topology derivation. |
| B3 | `PushPull.cpp:468-471, 762-765` (DCM T2OtherFET uses min secondary current) and `AsymmetricHalfBridge.cpp:880` (CD interval-A starts from ILo2_max) | Possible wrong variable in DCM/CD waveform reconstruction. |
| B4 | `AsymmetricHalfBridge.cpp:175-178` | `Vin_nom` from nested `value_or(0.0)` → division by zero downstream if input voltage incompletely specified. Throw instead? |
| B5 | `Zeta.cpp:34` | Duty tolerance `D >= Dmax − 0.01` (subtractive, rejects below ceiling) vs Cuk/Sepic `D > Dmax × 1.01` (multiplicative, allows slight overshoot). Comment claims they mirror each other; the math differs. |
| B6 | `Src.cpp:713` | Dead time `min(1% of half-period, 50 ns)` — at low fsw the 50 ns cap silently violates the documented 1% target. |
| B7 | `PhaseShiftedFullBridge.cpp:380` (0.7) vs `PhaseShiftedHalfBridge.cpp:340` (0.75) | Different fallback duty commands, undocumented. |
| B8 | `IsolatedBuck.cpp:355` | `maximumCurrentRiple = (...) * 2` — unexplained factor of 2. |
| B9 | `Cuk.cpp:512,522,541` (hardcoded turnsRatio 1.0 in non-isolated path) and `:121,134` (efficiency-scaled IL1avg compared against raw IsMax) | Unit/intent check. |
| B10 | `Settings.h` vs `Settings::reset()` | Drift: `_coilMaximumLayersPlanar` 4 (header) vs 32 (reset); `_coilAdviserMaximumNumberWires` 100 vs 30. Also reset() never restores `_coreLossesModelNames`, `_gappingStrategy`, `_nanocrystallineStackingFactor`, `_usePowderCores`, `_circuitSimulator*`. Which values are intended? |
| B11 | `MagneticField.cpp:447,462,505` | Fringing gated on exact `==` between harmonic frequency and excitation frequency — any resampling silently disables fringing. Tolerance? |
| B12 | `Bobbin.cpp:99,140,160` (ETD/PQ/EC use full `d2`) vs `:58,78,120` (RM/EP/PM use `d2/2`) | INVESTIGATED (June 2026): consumers (`Coil.cpp:8068`, `:6500`) treat `coordinates[0]` as the window CENTER (`left = coords[0] - width/2`), and `get_winding_window_coordinates` returns the stored value raw. The correct center is `d2/2 + (d1-d2)/4`; RM/EP/PM store the touch point `d2/2` (schema convention, shifted INWARD by width/2 under center semantics) and ETD/PQ/EC store the full `d2` (shifted outward). So all three (touch point, full diameter, assumed center) are mutually inconsistent; designs presumably work because the common placement path recomputes from the core. NEXT STEP: build one design per family (ETD vs RM), compare section x-placement + SVG, then pick ONE convention (suggest: store the true center, since that is what consumers assume) and fix all six processors together with `CorePiece` U/C/UR/UT. |
| B13 | `MagneticFilter.cpp:244` + `WireAdviser.cpp:343` (invert=true) | Solid-insulation scoring rewards maximum over-insulation; probable sign error but interacts with adviser tie-breaks. |
| B14 | `MagneticAdviser.cpp:509-537` | Section/margin dedupe only records combos once the per-core cap is hit (mostly dead code); no-toroids retry pushes unsimulated results. |
| B15 | `CoreAdviser.cpp:240-249` | Batched overload `std::move`s cores out of the caller's vector (potentially the global core database) — API footgun. |
| B16 | NL5 exporter | Missing from `CircuitSimulatorExporterModel::factory` (its model class doesn't derive from the base); ROSANO/ROSANO_RLC modes emit Rdc-only windings (`Nl5.cpp:139,288`); Simba ignores `mode` except FRACPOLE (`Simba.cpp:435`). |
| B17 | `Interface.cpp:1468,1517` | Mutual-resistance sign is a no-op (same sign applied to both aux couplings cancels in the product). Needs the Hesterman formulation checked. |
| B18 | `Inputs.cpp:690` (sinusoidal create_waveform emits closed [0,T] array) and `:1768` (`back()-front()` frequency) | Belongs with the pending `fix/waveform-period-half-open-convention` branch — fix there, not on main. |
| B19 | `MagneticSimulator.cpp:30-35,95-110` | Core-loss convergence loop persists the converged core temperature into the operating point's *ambient* condition, which is then stored back into the returned Mas inputs. Intended coupling? |
| B20 | `Core.cpp:189-203` | `interp()` dedup can shrink to <3 points (duplicate temperatures) → `tk::spline` assert/UB. Needs a 2-point linear path or a throw. |
| B20b | `Utils.cpp:1489,1532` | Bessel series uses FLOAT `tgammaf` in a double computation. Replacing it with double `tgamma` is NOT safe as-is: the float overflow at k≈34 is what truncates the series before the complex `pow(aux,k)` overflows; with `tgamma` the high-frequency (large-argument) path returns (0,0) and Ferreira proximity losses NaN (reproduced via Test_CircuitSimulatorExporter_Simba_Json_Ur at 6.2 MHz). The series itself is unsuitable for large arguments — needs an asymptotic/large-z algorithm, not a precision swap. |
| B22 | TestMagneticAdviser order-dependence | Running the `[magnetic-adviser]` tag standalone fails 21/77 tests **on pristine origin/main** (identical count on the fixed branch); in the full smoke suite the same binary flapped between passing and failing `Test_MagneticAdviser_Inductor_Only_Toroidal_Cores` and `Test_FastAdviser_LLC_Lm_Zero` across two consecutive runs. The tests depend on state left by earlier tests — prime suspects are the `Settings::reset()` omissions (B10) and lazy database loading. Worth fixing the tests' setup so they are self-contained. |
| C1 | `LeakageInductance.cpp:194-195` | Second-pass physics finding (June 2026, deferred): the inducing field values are one-sided FFT PEAK amplitudes, so `2*E/Irms^2` may give 2x the true leakage (the integral is the peak instantaneous energy = 0.5*L*Ipk^2). HOWEVER TestLeakageInductance passes within 30% against pinned expectations, so either those were calibrated to this output or a compensation exists. Needs a deliberate FEA/measurement check before changing — this would shift ALL leakage results by 2x. |
| C2 | `MagneticField.cpp:683-737` | Wang model "bottom"/"top" inducing blocks are unrotated copies of "left"/"right" (use distanceY/Hy where the rotated geometry needs distanceX/Hx); dead `(current - Hy*2*h)` term; missing else-throw in "right"/"top". Needs model surgery + validation. |
| C3 | `Reluctance.cpp:612` | Stenglein aux2 disagrees with its own cited Eq. 11 ((c^2+2cb+3b^2)/rc^2 vs code (c^2+2cb+b^2)/b^2), plus hardcoded 1 mm bobbin allowance and rx = column radius. Verify against Stenglein paper before touching. |
| C4 | `Reluctance.cpp:210` | Zhang model silently clamps distance-to-normal-surface up to the column width (inflates fringing for low-profile/planar cores). Policy clamp; check Zhang Eq. 10 intent. |
| C5 | `MagnetizingInductance.cpp:129,132` | Both DC-bias convergence loops hard-limited to ONE iteration (externalTimeout = internalTimeout = 1); the 10% convergence tests are dead code. If deliberate perf, delete the scaffolding; else raise the timeouts. |
| C6 | `WindingProximityEffectLosses.cpp:1195-1218` | Vandelac 3F1-4F2 combination does not vanish at DC (embeds transport loss already counted elsewhere) and the small-p guard (F1=1, F2=0.5) is mathematically false, creating a ~1e4 discontinuity at p=1e-4. Needs model-intent decision (F1-2F2 would be pure proximity). |
| C7 | `CoreLosses.cpp:746-749` etc. | `get_magnetic_flux_density_peak(excitation, f) - offset` double-subtracts DC when the helper already returned the harmonic amplitude, then fabs() masks the sign. Fix requires the helper to RETURN which convention it used. |
| C8 | `MagneticField.cpp:429-435` | Albach skin-depth subdivision uses winding 0's wire for all windings (accuracy only; needs per-winding updateSkinDepths API). |
| C9 | `LeakageInductance.cpp:179-183` | Toroidal leakage turn-length mixes polar conventions (coords[0]+bobbinColumnWidth as a radius). Verify against a known toroid case. |
| C10 | `Inputs.cpp:603-610` | RECTANGULAR_WITH_DEADTIME levels (max=pp(1-D), min=-pp*D) cannot satisfy volt-second balance when deadTime > 0 — Buck/Boost DCM now bypass it with explicit CUSTOM waveforms; other users of this label with dead time have the same latent imbalance. |
| B21 | Misc unguarded-UB sites (medium confidence / unclear reachability) | `Flyback.cpp:1082` empty operating points; `Coil.cpp:4281` layerPhysicalTurns OOB when explicit number_layers > computed layers; `Coil.cpp:4708,5054,5258` unbounded all-zero-proportion scans; `Coil.cpp:4310` layer perimeter uses wire diameter instead of layer position; `Coil.cpp:7372` polar insulation-thickness inverse uses radius 0; `Bobbin.cpp:354+` load_interpolators OOB on empty aux vectors; `normalize_scoring` log10(0) → ±inf passes the NaN check; `to_title_case` empty string; `find_closest_index` empty vector; `Logger._level` unsynchronized read; `CoilMesher.cpp:337,437` map `operator[]` default-inserts while iterating by size; `Insulation.cpp:11` linear_table_interpolation silently extrapolates standards tables; `NumberTurns.cpp:30,39` div-by-zero/modulo-zero; `InsulationMaterial.cpp:104-113` OOB on empty data + unchecked optional; `Insulation.cpp:120,144` unchecked coating optionals; `Inputs.cpp` try_guess_duty_cycle unguarded breakpoint indexing on sampled waveforms (:341+), calculate_instantaneous_power size mismatch (:3353), CMC magnetizing current offset contradicts its own comment (:946), prune paths don't force-keep the fundamental. |
